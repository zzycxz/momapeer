# MoMAPeer 功能全景

> MoMAPeer 是面向中国移动九天大模型平台的企业级全场景 AI 助手，基于 Wails 桌面框架（Go 后端 + React 前端）构建。单一静态二进制，零运行时依赖，支持 Windows / macOS / Linux。

---

## 架构总览

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户入口                                  │
│  ┌──────┐  ┌──────────┐  ┌──────┐  ┌────────┐  ┌──────┐       │
│  │ CLI  │  │ Desktop  │  │ HTTP │  │ IM Bot │  │ ACP  │       │
│  │ TUI  │  │ (Wails)  │  │ /SSE │  │飞书/微信│  │远程  │       │
│  └──┬───┘  └────┬─────┘  └──┬───┘  └───┬────┘  └──┬───┘       │
│     └───────────┴───────────┴──────────┴──────────┘            │
│                           │                                     │
│              ┌────────────▼────────────┐                       │
│              │   Controller (会话驱动)   │                       │
│              └────────────┬────────────┘                       │
│                           │                                     │
│              ┌────────────▼────────────┐                       │
│              │   Agent (ReAct 循环)     │                       │
│              │  思考 → 工具调用 → 解析   │                       │
│              └─────┬──────────┬────────┘                       │
│                    │          │                                  │
│         ┌──────────▼──┐  ┌───▼──────────┐                     │
│         │ Provider    │  │ Tool Registry │                     │
│         │ (模型适配)   │  │ (工具注册表)   │                     │
│         └─────────────┘  └──────────────┘                     │
└─────────────────────────────────────────────────────────────────┘
```

**五种运行模式：**

| 模式 | 命令 | 说明 |
|------|------|------|
| 终端 TUI | `momapeer chat` | Charm Bubble Tea 沉浸式终端 |
| API 服务 | `momapeer serve` | HTTP/SSE 编程接口 |
| 桌面客户端 | 双击图标启动 | 原生多标签页 UI |
| IM 机器人 | `momapeer bot start` | 飞书/微信/QQ 网关 |
| ACP 服务 | `momapeer acp` | Agent Control Protocol 远程控制 |

---

## 1. AI 对话

### 多模型支持

内置 300+ 模型预设，通过九天 MoMA 平台统一接入：

```
┌─────────────────────────────────────────────────────┐
│  九天系列    │ jiutian-lan-236b / 35b / thinking     │
│  通义千问    │ qwen3.6-35b / 27b, qwen3.5-397b      │
│  DeepSeek   │ deepseek-v4-flash                      │
│  智谱 GLM   │ glm-5.1 / 5.2                          │
│  MiniMax    │ minimax-m2.7 / m2.5                     │
│  Kimi       │ kimi-k2.6 / k2.5-thinking              │
└─────────────────────────────────────────────────────┘
```

每个 provider 支持 `models` 数组多模型共享，`models_url` 自动拉取模型列表。

### 思维链展示

支持 `reasoning_content` 协议，思维过程可展开/折叠：

```
┌─────────────────────────────────────────┐
│ 🤔 思考中...                    [展开 ▼] │
│ ┌─────────────────────────────────────┐ │
│ │ 让我分析一下这个问题...              │ │
│ │ 首先需要理解题目的意思...            │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ 答案是 42。                              │
└─────────────────────────────────────────┘
```

- `expand_thinking`：默认展开/折叠
- `reasoning_language`：思维语言（auto/zh/en）独立于回答语言

### 会话管理

- 多标签页，每个标签独立项目空间
- 上下文压缩：50% 提示 → 80% 触发压缩 → 90% 强制压缩
- 记忆系统：`remember` / `forget` / `recall` 工具 + 自动记忆
- Dream（7天）自动整合会话知识 → 项目记忆
- Distill（30天）自动发现重复工作流 → 打包为可复用技能

---

## 2. 编程工具

### 核心工具（始终可用）

```
┌────────────────────────────────────────────────────────┐
│  bash          │ 执行 shell 命令，支持超时和沙箱        │
│  read_file     │ 读取文件内容 / 列出目录                │
│  write_file    │ 写入文件（限制在工作区内）              │
│  edit_file     │ 编辑文件（模糊匹配）                   │
│  multi_edit    │ 批量编辑多处                           │
│  grep          │ 搜索文件内容（ripgrep 或原生 Go）      │
│  glob          │ 文件模式匹配                           │
│  web_fetch     │ 抓取 URL 内容（代理感知，SSRF 防护）   │
│  web_search    │ 网页搜索（Brave → Exa → Linkup 链）    │
│  apply_patch   │ 应用 unified diff 补丁                 │
│  todo_write    │ 任务列表追踪                           │
│  task          │ 生成子 agent                           │
│  remember      │ 自动记忆 CRUD                          │
└────────────────────────────────────────────────────────┘
```

### 代码智能

| 工具 | 说明 |
|------|------|
| `codegraph_context` | 入口点 + 关联符号 + 关键代码 |
| `codegraph_search` | 按名称/模式搜索符号 |
| `lsp_definition` | 跳转定义 |
| `lsp_references` | 查找引用 |
| `lsp_hover` | 悬停信息 |
| `lsp_diagnostics` | 诊断信息 |

支持 Go / Python / TypeScript / Rust / Java / C/C++ 的 LSP 规范。

---

## 3. 办公自动化（CoWork 模式）

切换到办公模式后，激活以下桌面自动化工具：

### 屏幕操作工具

```
┌──────────────────────────────────────────────────────────────┐
│                    屏幕工具链                                  │
│                                                              │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐      │
│  │ screenshot  │    │ screen_click│    │ screen_type │      │
│  │ 全屏/区域截图│    │ 点击坐标    │    │ 输入文字    │      │
│  └──────┬──────┘    └──────┬──────┘    └──────┬──────┘      │
│         │                  │                  │              │
│  ┌──────▼──────┐    ┌──────▼──────┐    ┌──────▼──────┐      │
│  │screen_perceive│   │ screen_key │    │screen_scroll│      │
│  │ UIA+VLM感知  │    │ 发送快捷键 │    │ 滚动页面    │      │
│  └─────────────┘    └─────────────┘    └─────────────┘      │
│                                                              │
│  ┌─────────────┐    ┌─────────────┐                         │
│  │ get_ui_tree │    │window_focus │                         │
│  │ UIA元素树   │    │ 窗口管理    │                         │
│  └─────────────┘    └─────────────┘                         │
└──────────────────────────────────────────────────────────────┘
```

- **screen_perceive**：UIA + VLM 融合感知，返回带元素 ID 标注的截图 + 坐标
- **窗口管理**：focus / maximize / restore / move / close
- **computer-auto 技能**：感知 → 操作 → 验证循环，驱动 WPS/Excel 等原生应用

### 快捷截屏解题（考试助手）

> 使用场景：做题时截屏受限，想偷偷让 AI 解题并把结果发回来。按一下热键，AI 自动识别题目、给出答案、验证结果，通过飞书/微信推送。

**设置面板（设置 → 办公 → 快捷截屏）：**

```
┌─────────────────────────────────────────────────────────────┐
│  快捷截屏                                                    │
│                                                             │
│  图片理解模型        [qwen/qwen3.5-397b-a17b ▾]             │
│  全局图片识别的主模型。失败时自动降级到九天兜底。              │
│                                                             │
│  启用快捷截屏        [✓]                                    │
│  开启后，按快捷键截屏 → AI 解题 → 结果发 IM + 弹窗          │
│                                                             │
│  快捷键              [ Ctrl+Shift+Alt+W ] [录制]            │
│  组合键，修改后保存并重启生效。                              │
│                                                             │
│  解题提示词                                                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 请从屏幕截图中找到问题，给出答案，再说明解题过程……   │   │
│  └─────────────────────────────────────────────────────┘   │
│  发送给图片理解模型的指令。可自定义解题风格。                 │
│                                    [恢复默认] [保存提示词]   │
└─────────────────────────────────────────────────────────────┘
```

**运行流程：**

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   用户按 Ctrl+Shift+Alt+W（任意应用前台）                    │
│         │                                                   │
│         ▼                                                   │
│   ┌───────────────┐   Win32 BitBlt 全屏截图                 │
│   │ CaptureScreen  │   1920×1080, ~200KB PNG                │
│   └───────┬───────┘                                         │
│           ▼                                                 │
│   ┌───────────────┐   图片 + 提示词 → VLM 模型              │
│   │  solveScreenshot│  四步结构：                            │
│   │  ① 识别题目    │  ① 识别题目 → ② 答案 → ③ 过程 → ④ 验证│
│   │  ② 给出答案    │                                       │
│   │  ③ 解题过程    │  有搜索 key → mini-agent 联网搜索      │
│   │  ④ 验证结果    │  无搜索 key → 纯 VLM 单轮             │
│   └───────┬───────┘                                         │
│           ▼                                                 │
│   ┌───────────────┐   飞书/微信 IM 推送 + 前端 toast        │
│   │  IM Push       │   🧮 截图解题结果：                     │
│   └───────────────┘                                         │
│                                                             │
│   触发方式：                                                 │
│   • 键盘热键：GetAsyncKeyState 轮询（50ms）                  │
│   • 托盘菜单：右键 → 📷 截图解题                             │
│                                                             │
│   配置项：                                                   │
│   • screenshot_enabled      开关（默认关闭）                │
│   • screenshot_hotkey       热键（默认 Ctrl+Shift+Alt+W）   │
│   • screenshot_vlm_model    模型（默认 qwen3.5-397b-a17b）  │
│   • screenshot_prompt       提示词（用户可自定义）           │
└─────────────────────────────────────────────────────────────┘
```

### 紧急停止（E-Stop）

`Ctrl+Shift+Pause`（可配置）— 桌面自动化运行时的紧急刹车，取消进行中的任务。

---

## 4. IM 机器人集成

```
┌─────────────────────────────────────────────────────────┐
│                    Bot Gateway                           │
│                                                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐             │
│  │  飞书    │  │  微信    │  │   QQ     │             │
│  │ Webhook  │  │  iLink   │  │ Bot API  │             │
│  │WebSocket │  │          │  │   v2     │             │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘             │
│       └──────────────┼──────────────┘                  │
│                      │                                  │
│              ┌───────▼───────┐                         │
│              │  BotGateway   │                         │
│              │ • 会话管理     │                         │
│              │ • 消息路由     │                         │
│              │ • 白名单       │                         │
│              │ • 防抖窗口     │                         │
│              └───────┬───────┘                         │
│                      │                                  │
│              ┌───────▼───────┐                         │
│              │  Controller   │  复用主 agent 能力       │
│              └───────────────┘                         │
└─────────────────────────────────────────────────────────┘
```

**支持平台：**
- **飞书/Lark**：Webhook 或 WebSocket
- **微信 iLink**：iLink Bot API
- **QQ 官方**：QQ Bot API v2

**im_send 工具**：AI 对话中可主动推送消息到已连接的 IM 会话。

---

## 5. 专家团协作

```
┌─────────────────────────────────────────────────────────┐
│                   Expert Team                           │
│                                                         │
│  三种协作模式：                                          │
│                                                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │   Parallel  │  │    Debate   │  │   Pipeline  │    │
│  │   并行分析   │  │    辩论     │  │   流水线    │    │
│  │             │  │             │  │             │    │
│  │ 专家A ──┐   │  │ 专家A ←──→  │  │ 专家A →    │    │
│  │ 专家B ──┼→ 合│  │ 专家B ←──→  │  │ 专家B →    │    │
│  │ 专家C ──┘   │  │ 专家C ←──→  │  │ 专家C →    │    │
│  │             │  │  多轮交锋   │  │  顺序执行   │    │
│  └─────────────┘  └─────────────┘  └─────────────┘    │
│                                                         │
│  每个专家：名称 + 模型 + 视角                            │
│  可选：联网搜索 / 知识库检索 / 跳过综合                   │
└─────────────────────────────────────────────────────────┘
```

---

## 6. 技能系统

```
┌─────────────────────────────────────────────────────────┐
│                   Skill Engine                          │
│                                                         │
│  发现来源：                                              │
│  • 内置（随二进制发布）                                   │
│  • 项目（.momapeer/skills/）                             │
│  • 全局（~/.momapeer/skills/）                           │
│  • 自定义路径（config 配置）                              │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │  15 个内置技能                                    │   │
│  │                                                  │   │
│  │  init           │ 项目初始化                      │   │
│  │  explore        │ 代码库探索                      │   │
│  │  research       │ Web + 代码联合调研              │   │
│  │  review         │ 代码审查                        │   │
│  │  security-review│ 安全审查                        │   │
│  │  test           │ 测试运行 + 修复                 │   │
│  │  browser-auto   │ 浏览器自动化                    │   │
│  │  computer-auto  │ 桌面应用自动化                  │   │
│  │  email-auto     │ 邮件收发                        │   │
│  │  rag-auto       │ 知识库操作                      │   │
│  │  schedule-auto  │ 定时任务管理                    │   │
│  │  document-auto  │ 文档读写转换                    │   │
│  │  expert-auto    │ 专家团协作                      │   │
│  │  ppt-auto       │ PPT 生成                        │   │
│  │  install-capability │ 安装 MCP/技能               │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  冷退休：90天未用标记 [dormant]，2x 阈值后禁用           │
└─────────────────────────────────────────────────────────┘
```

---

## 7. RAG 知识库

```
┌─────────────────────────────────────────────────────────┐
│                   Knowledge Base                        │
│                                                         │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐         │
│  │ 文档导入  │    │ 深度抽取  │    │ 知识图谱  │         │
│  │md/docx/  │───→│ LLM 分块 │───→│ 实体+关系 │         │
│  │pdf/xlsx  │    │ 抽取     │    │ 图谱     │         │
│  └──────────┘    └──────────┘    └──────────┘         │
│                                                         │
│  检索方式：                                              │
│  • SQLite FTS5 全文搜索（CJK 分词）                      │
│  • 结构化实体/关系图谱搜索                                │
│  • 可选语义重排序（embedding 模型）                       │
│                                                         │
│  工具：                                                  │
│  • rag_search  │ 搜索知识库                              │
│  • rag_import  │ 导入文件/文件夹                         │
│  • rag_list    │ 列出集合/文档                           │
│  • rag_delete  │ 删除集合/文档（需审批）                  │
│  • rag_mindmap │ 生成思维导图                            │
│                                                         │
│  支持：Obsidian vault 导入                               │
└─────────────────────────────────────────────────────────┘
```

---

## 8. 浏览器自动化

```
┌─────────────────────────────────────────────────────────┐
│                Browser Automation                       │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │  Chromedp (CDP) — 15 个浏览器工具               │   │
│  │                                                  │   │
│  │  browser_open      │ 启动 Chromium               │   │
│  │  browser_attach    │ 附加到已有浏览器             │   │
│  │  browser_navigate  │ 导航到 URL                  │   │
│  │  browser_click     │ 点击元素                    │   │
│  │  browser_type      │ 输入文字                    │   │
│  │  browser_scroll    │ 滚动页面                    │   │
│  │  browser_extract   │ 提取内容                    │   │
│  │  browser_screenshot│ 页面截图                    │   │
│  │  browser_evaluate  │ 执行 JavaScript             │   │
│  │  browser_snapshot  │ 无障碍树 + 元素引用         │   │
│  │  browser_auto      │ 自主浏览（browser-use）     │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  browser-use 集成：                                      │
│  Python sidecar，自主循环：                              │
│  快照页面 → 问 LLM → 执行操作 → 重复                    │
└─────────────────────────────────────────────────────────┘
```

---

## 9. 邮件集成

```
┌─────────────────────────────────────────────────────────┐
│                   Email Integration                     │
│                                                         │
│  多账号配置：                                            │
│  [[cowork.email_accounts]]                              │
│  • 个人 139 邮箱                                        │
│  • 企业 CMCC 邮箱                                       │
│  • ...                                                  │
│                                                         │
│  ┌──────────┐    ┌──────────┐                          │
│  │ SMTP 发送 │    │ IMAP 收取 │                          │
│  │ CC/BCC   │    │ 未读筛选  │                          │
│  │ 附件     │    │ 时间范围  │                          │
│  │ HTML     │    │ 服务端搜索│                          │
│  └──────────┘    └──────────┘                          │
│                                                         │
│  安全：DPAPI 加密存储密码                                │
│  设置页：IMAP 连接测试                                   │
└─────────────────────────────────────────────────────────┘
```

---

## 10. 日历与定时任务

### 日历

- 事件 CRUD（创建/列表/更新/删除/搜索/忙闲查询）
- ICS 导入导出
- RRULE 循环事件
- 节假日感知
- 提醒引擎（toast / IM / 邮件）

### 定时任务

```
┌─────────────────────────────────────────────────────────┐
│                   Scheduler                             │
│                                                         │
│  表达式格式：                                            │
│  • every 30m          │ 每 30 分钟                      │
│  • daily 09:00        │ 每天 9 点                       │
│  • daily 09:00 Mon-Fri│ 工作日 9 点                     │
│  • at 2026-06-24 15:00│ 一次性                          │
│  • in 2h30m           │ 相对时间                        │
│  • 5 字段 cron        │ 标准 cron                       │
│                                                         │
│  输出路由：IM / 邮件 / 文件 / 桌面通知                   │
│  一次性任务执行后自动禁用                                │
│  应用重启后恢复                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 11. PPT 生成

- 模板系统：`ppt_active_template` 选择模板
- 两种模式：`fast`（单轮）/ `validate`（生成+检查+返工）
- CUA 驱动：像人一样操作 WPS Presentation（打开→感知→点击→输入）
- 嵌入二进制，首次运行释放到 `~/.momapeer/skills/ppt-auto/`

---

## 12. 设置面板

13 个标签页，覆盖所有配置：

```
┌─────────────────────────────────────────────────────────────┐
│  设置                                                        │
│  ┌──────┬──────┬──────┬──────┬──────┬──────┬──────┐        │
│  │ 通用 │ 模型 │ 机器人│ 办公 │ MCP │ 技能 │ 记忆 │        │
│  ├──────┼──────┼──────┼──────┼──────┼──────┼──────┤        │
│  │ 权限 │ 沙箱 │ 网络 │ 钩子 │ 外观 │ 更新 │      │        │
│  └──────┴──────┴──────┴──────┴──────┴──────┴──────┘        │
│                                                             │
│  通用：语言、默认模型、系统提示词、最大步数、温度             │
│  模型：Provider 管理、模型选择、effort/thinking/vision       │
│  机器人：飞书/微信/QQ 连接、QR 码扫描、白名单               │
│  办公：浏览器、PPT、邮件（多账号）、RAG、截屏热键、E-Stop   │
│  MCP：MCP 服务器管理、搜索引擎配置                           │
│  技能：技能启用/禁用、自定义路径、九天多模态工具              │
│  记忆：用户画像、自动记忆、项目文档                          │
│  权限：基于主体的权限策略（glob 匹配）                       │
│  沙箱：Bash 沙箱模式、写入/读取根、网络出口                  │
│  网络：代理模式（auto/env/custom/off）                       │
│  钩子：PreToolUse / PostToolUse / UserPromptSubmit / Stop   │
│  外观：主题（12 种风格）、字号、字体、显示模式               │
│  更新：更新检查、遥测、指标、配置路径                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 13. 系统托盘

```
┌─────────────────────────────────────────┐
│  ┌───┐                                  │
│  │ M │  左键点击 → 打开主窗口           │
│  └───┘  右键菜单：                      │
│         ┌──────────────────────┐        │
│         │ 📷 截图解题           │        │
│         │ 打开                  │        │
│         │ 退出                  │        │
│         └──────────────────────┘        │
│                                         │
│  关闭窗口 → 最小化到托盘（可配置）       │
│  中英文双语 locale                      │
└─────────────────────────────────────────┘
```

---

## 14. 安全与权限

### 权限系统

```
┌─────────────────────────────────────────────────────────┐
│  规则格式：subject pattern → tool pattern → allow/ask/deny│
│                                                         │
│  示例：                                                  │
│  • * → bash → ask          （所有 bash 需审批）          │
│  • * → write_file → allow  （写文件自动允许）            │
│  • * → email_send → ask    （发邮件需审批）              │
│  • * → rag_delete → ask    （删知识库需审批）            │
│                                                         │
│  glob 匹配：* 通配符，deny 规则不可绕过                  │
└─────────────────────────────────────────────────────────┘
```

### 沙箱

- Bash 沙箱模式：限制文件系统访问
- 写入根 / 读取根：限制操作范围
- 网络出口：控制网络访问
- `filepath.IsLocal` 防止路径穿越

### 检查点 / 回滚

代码修改自动快照，`/rewind` 一键回滚到任意检查点。

---

## 15. MCP 插件生态

```
┌─────────────────────────────────────────────────────────┐
│  Model Context Protocol (MCP)                           │
│                                                         │
│  外部工具作为子进程接入：                                │
│  • stdio 传输                                           │
│  • HTTP/SSE 传输                                        │
│                                                         │
│  启动策略：                                              │
│  • eager    │ 阻塞启动                                  │
│  • lazy     │ 首次使用时启动                             │
│  • background│ 后台立即启动                              │
│                                                         │
│  自动降级：慢服务器自动降级                              │
│  热添加：/mcp add 动态添加                              │
└─────────────────────────────────────────────────────────┘
```

---

## 16. 自我进化

### Dream（7天周期）

自动整合会话知识 → 项目记忆。在空闲时运行，使用 `fast_task_model`（默认 qwen3.6-35b）降低成本。

### Distill（30天周期）

自动发现重复工作流 → 打包为可复用技能。从历史会话中提取模式，生成 SKILL.md。

### 冷退休

技能 90 天未使用标记为 `[dormant]`，2x 阈值后自动禁用。

---

## 17. 配置文件

**格式**：TOML

**加载顺序**：命令行 flag > 项目 `./momapeer.toml` > 用户 `~/.config/momapeer/config.toml` > 内置默认

**密钥**：通过 `api_key_env` 从环境变量读取，不写入配置文件

```toml
# 示例配置片段
default_model = "minimax/minimax-m2.7"
language = "zh"

[desktop]
theme = "dark"
theme_style = "graphite"
close_behavior = "background"

[cowork]
screenshot_enabled = true
screenshot_hotkey = "Ctrl+Shift+Alt+W"
screenshot_vlm_model = "qwen/qwen3.5-397b-a17b"
screenshot_prompt = "请从屏幕截图中找到问题，给出答案..."

[[providers]]
name = "moma"
kind = "openai"
base_url = "https://jiutian.10086.cn/largemodel/moma/api/v3"
models = ["qwen/qwen3.6-35b", "qwen/qwen3.5-397b-a17b"]
api_key_env = "JIUTIAN_API_KEY"
```

---

## 18. 国际化

完整中英文双语支持：
- 桌面 UI（设置面板、托盘菜单、toast 通知）
- CLI（所有工具描述、帮助文本）
- 工具输出（错误信息、状态提示）

`language` 配置项或自动检测（`$LANG` / `$MOMAPEER_LANG`）。

---

*文档版本：v0.5.15 — 2026-08-02*
