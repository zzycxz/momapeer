# ppt-auto skill dependencies / ppt-auto 技能依赖
# ============================================
# 跨平台安装（macOS/Linux/Windows）：
#   python3 -m pip install -r requirements.txt
#
# 本 skill 不再携带嵌入式 Python 运行时，改用系统 Python 3.10+。
# 大部分依赖为纯 Python，无需安装 Office/WPS。

# ── 核心依赖（SVG → PPTX 主流程必需）──────────────────────────
# svg_to_pptx.py / template_fill_pptx.py 直接拼 DrawingML，无需 Office
python-pptx>=0.6.21

# SVG → PNG 后备渲染（Office 兼容模式）
# macOS 需额外：brew install cairo
cairosvg>=0.5.0

# 图片处理（尺寸校正、水印去除、宽高比修复）
Pillow>=9.0.0
numpy>=1.20.0

# ── 源文档提取（extract_content.py，可选）────────────────────
# 仅在用户提供 PDF/DOCX 等源文档时需要
PyMuPDF>=1.23.0          # PDF → 文本/图片/表格（import 名为 fitz）
mammoth>=1.6.0           # .docx → markdown
openpyxl>=3.1.0          # .xlsx → markdown

# ── 编码探测（console_encoding.py，可选，缺失会优雅降级）──────
chardet>=5.0.0

# ── Windows 专属（可选）──────────────────────────────────────
# analyze_template.py / export_pdf.py / export_previews.py 通过 COM
# 调用 PowerPoint 做模板分析/PDF 导出。仅 Windows 且装有 Office 时生效；
# macOS/Linux 会自动跳过这些路径。无需 Office 的纯 python-pptx 路线不受影响。
# comtypes>=1.2.0        # Windows only: uncomment on Windows + Office
