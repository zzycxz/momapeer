name: Desktop Release

on:
  push:
    tags:
      - 'desktop-v*'

permissions:
  contents: write

jobs:
  build:
    name: Build Desktop App
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            platform: windows/amd64
            args: -nsis
            artifact: momapeer-amd64-installer.exe
          - os: macos-latest
            platform: darwin/universal
            args: ''
            artifact: momapeer.app
          - os: ubuntu-22.04
            platform: linux/amd64
            args: ''
            artifact: momapeer
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install pnpm
        run: npm i -g pnpm

      - name: Install Linux Dependencies
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.0-dev

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Build Wails App
        working-directory: ./desktop
        run: wails build -platform ${{ matrix.platform }} -clean ${{ matrix.args }}

      - name: Compress macOS app
        if: runner.os == 'macOS'
        working-directory: ./desktop/build/bin
        run: zip -r momapeer-macos-universal.zip momapeer.app

      - name: Upload Windows Artifact
        if: runner.os == 'Windows'
        uses: softprops/action-gh-release@v2
        with:
          files: ./desktop/build/bin/momapeer-amd64-installer.exe

      - name: Upload macOS Artifact
        if: runner.os == 'macOS'
        uses: softprops/action-gh-release@v2
        with:
          files: ./desktop/build/bin/momapeer-macos-universal.zip

      - name: Upload Linux Artifact
        if: runner.os == 'Linux'
        uses: softprops/action-gh-release@v2
        with:
          files: ./desktop/build/bin/momapeer
